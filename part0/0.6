sequenceDiagram
    participant browser
    participant server

    Note right of browser: User writes note and clicks Save

    Note right of browser: JavaScript prevents default form submit

    browser->>server: POST /exampleapp/new_note_spa (JSON)
    activate server
    server-->>browser: 201 Created / OK response
    deactivate server

    Note right of browser: JS updates note list on page instantly
    Note right of browser: No page reload happens
